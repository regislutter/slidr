var React = require('react');
var PropTypes = React.PropTypes;

function ContactInfo(props) {
    return (
        <div className="slide">Info contact</div>
    )
};

ContactInfo.propTypes = {}

module.exports = ContactInfo;